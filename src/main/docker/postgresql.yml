version: '2'
services:
  mjk-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/mjk/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=mjk
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
